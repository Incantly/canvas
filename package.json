{
  "name": "incantly-canvas-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Incantly Canvas — the MIT-licensed infinite-canvas whiteboard SDK. Monorepo root.",
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/react-demo",
    "apps/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "npm run build:packages",
    "build:packages": "npm run build --workspace=@incantly/canvas && npm run build --workspace=@incantly/canvas-react && npm run build --workspace=@incantly/canvas-react-native",
    "dev": "npm run dev --workspace=incantly-react-demo",
    "dev:playground": "npm run dev --workspace=incantly-playground",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "typecheck:packages": "npm run typecheck --workspace=@incantly/canvas && npm run typecheck --workspace=@incantly/canvas-react && npm run typecheck --workspace=@incantly/canvas-react-native",
    "star-history": "node scripts/star-history.mjs",
    "publish:all": "npm publish --workspace=@incantly/canvas --access public && npm publish --workspace=@incantly/canvas-react --access public && npm publish --workspace=@incantly/canvas-react-native --access public"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.0",
    "esbuild": "^0.25.0",
    "jsdom": "^26.0.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  },
  "license": "MIT",
  "allowScripts": {
    "esbuild@0.25.12": true,
    "esbuild@0.28.1": true
  }
}
